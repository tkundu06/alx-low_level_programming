#include "main.h"
#include <string.h>

/**
 * _strlen - Function to return string length using
 * strlen() function found in standard library
 * @s: pointer, variable to hold string
 *
 * Return: String length
 */
int _strlen(char *s)
{
	return (strlen(s));
}
